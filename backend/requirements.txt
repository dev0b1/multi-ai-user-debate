# LiveKit Agents and dependencies
livekit-agents>=1.0.0
python-dotenv>=1.0.0

# AI/ML dependencies
openai>=1.0.0
deepgram-sdk>=2.0.0

# Note: cartesia-tts and silero-vad are included in livekit-agents
# If you need standalone versions, install them separately 